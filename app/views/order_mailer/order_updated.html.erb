<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
</head>
<style>
  table {
    width: 300px;
    border-collapse: collapse;
  }
  td, th {
    padding: 3px;
    border: 1px solid black;
  }
  th {
    background: #b0e0e6;
  }
  .line {
    border: none;
    background-color: black;
    height: 2px;
  }
</style>
<body>
<h1> GeeKHub SHOP</h1>
<h2> <%= t('mailer.order.dear') %> <%= @order.full_name.capitalize %><%= t('mailer.order.order_changed') %></h2>
<div>
  <h3><%= t('mailer.order.order_id') %><b><%= @order.id %></b></h3>
  <br>
  <table>
    <tr>
      <th><%= t('mailer.order.product') %></th>
      <th><%= t('mailer.order.quantity') %></th>
      <th><%= t('mailer.order.price') %></th>
    </tr>
    <ul>
      <% @order.cart_items.each do |item| %>
        <tr>
          <td><%= item.product.title %></td>
          <td><%= item.quantity %></td>
          <td><%= item.price %></td>
        </tr>
      <% end %>
    </ul>
  </table>
  <h3><%= t('backoffice.orders.show.address') %> <%= @order.address.capitalize %></h3>
  <h2><%= t('mailer.order.total_price') %> <b><%= @order.total_price %></b></h2>
  <h1><%= t('backoffice.orders.index.status') %> <b><i><%= @order.status.capitalize %></i></b></h1>
  <hr class="line">
    <% if @order.admin_comment? %>
      <h4>GeekHub Shop:</h4>
      <p><%= @order.admin_comment %></p>
    <% end %>
  <hr class="line">
</div>
<div>
  <%= t('mailer.order.if_edit') %>
</div>
<p>
  <i><%= t('mailer.common.created_by') %></i>
</p>
</body>
</html>