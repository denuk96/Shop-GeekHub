<% @cart_items.each do |cart_item| %>
  <br>
  <%= cart_item.product.title %>
  <%= cart_item.product.description %>
  <%= link_to t('destroy'), cart_item_path(cart_item), method: :delete,
              data: { confirm: t('confirmation') } %>
  <br>
<% end %>